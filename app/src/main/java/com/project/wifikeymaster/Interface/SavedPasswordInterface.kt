package com.project.wifikeymaster.Interface

//interface for callback to SavedPassword fragment to display
// no saved pass icon when no item is present in the list
interface SavedPasswordInterface {
    fun noFileImageView(b:Boolean)
}